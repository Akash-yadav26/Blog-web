<!DOCTYPE html>
<html lang="en">
<head>
    <%- include('./partials/head') %>
    <title>Blog</title>
    <style>
        p{
            word-spacing: 20px;
        }
       
    </style>
</head>

<body>
<%- include('./partials/nav') %>
 <div class="container mt-3">
<h1 ><%= blog.title %></h1>
<img class="mt-3"    src="<%= blog.CoverImageURL %>" width="700px"/>
<p class="mt-3"><%= blog.body %></p>
</div>
 <div class="container mt-4">
    <img src="<%= blog.CreatedBy.profileImageURL %>" width="20px" > <%= blog.CreatedBy.fullName %>
 </div>
 <!-- we use locals becz locals have object which have all type of variabe which be used to check if server give this variable or not without any error -->
 <div class="container mt-3">
<h2>Comments (<%= comments.length %>)</h2>
<% if (locals.user) { %>
<form  action="/blog/comment/<%= blog._id %>" method="post">
     <div>
    <input type="text" class="form-control mb-3" id="email" name="content" placeholder="Enter your comment">
    <button class="btn btn-sm btn-primary mb-3" type="submit">Add</button>
     </div>
</form>

<% } %>
<div class="mt-3">
    <% comments.forEach(comment => { %>
      <img src="<%= comment.CreatedBy.profileImageURL %>" width="20px" > <%= comment.CreatedBy.fullName %>
      <pre><%= comment.content %></pre>

      <% }) %>
 </div>
 </div>

    <%- include('./partials/scripts') %>
</body>
</html>
