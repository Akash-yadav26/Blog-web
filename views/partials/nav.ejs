<nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
  <div class="container-fluid">
    <a class="navbar-brand fw-bold" href="/">Blogify</a>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent"
      aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarContent">
      <ul class="navbar-nav ms-auto d-flex align-items-center">

        <!-- Home Link -->
        <li class="nav-item me-2">
          <a class="btn btn-sm btn-outline-light" href="/">Home</a>
        </li>

        <% if (locals.user) { %>
          <!-- Add Blog -->
          <li class="nav-item me-2">
            <a class="btn btn-sm btn-outline-light" href="/blog/add-new">Add Blog</a>
          </li>

          <!-- Dropdown -->
          <li class="nav-item dropdown me-2">
            <a class="btn btn-sm btn-primary dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <%= locals.user.fullName || 'Account' %>
            </a>
            <ul class="dropdown-menu dropdown-menu-end">
              <li><a class="dropdown-item" href="/user/logout">Logout</a></li>
            </ul>
          </li>
        <% } else { %>
          <!-- Signin -->
          <li class="nav-item me-2">
            <a class="btn btn-sm btn-outline-light" href="/user/signin">Sign In</a>
          </li>
          <!-- Signup -->
          <li class="nav-item me-2">
            <a class="btn btn-sm btn-outline-light" href="/user/signup">Create Account</a>
          </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>

<% if (locals.error) { %>
  <div class="container mt-4">
    <div class="alert alert-danger shadow-sm" role="alert">
      <%= locals.error %>
    </div>
  </div>
<% } %>
